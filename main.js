const { Person } = require('./models');
const { Op } = require('sequelize');

const main = async () => {
  // write your queries here
  
  
}

const run = async () => {
  await main();
  process.exit();
}

run();
